# portal.taypro.in — block search indexing (SEO portal-noindex).
# Include in the portal subdomain server {} block.

add_header X-Robots-Tag "noindex, nofollow" always;

# Optional dedicated robots.txt for the subdomain:
# location = /robots.txt {
#     default_type text/plain;
#     return 200 "User-agent: *\nDisallow: /\n";
# }

export type BlogFeaturedImagePick = {
  url: string;
  alt: string;
  source: string;
  mode: "library" | "generated";
};

export type BlogInlineImage = {
  url: string;
  alt: string;
  source: string;
};
